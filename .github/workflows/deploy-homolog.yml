name: Deploy App ERP - Homologação

on:
  push:
    branches:
      - homolog

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to server
        env:
          VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_DEPLOY_PATH: ${{ secrets.VPS_DEPLOY_PATH }}
          # Variáveis de ambiente para homologação
          VITE_API_USUARIOS_BASE_URL_HOMOLOG: ${{ secrets.VITE_API_USUARIOS_BASE_URL_HOMOLOG }}
          VITE_API_CLIENTES_BASE_URL_HOMOLOG: ${{ secrets.VITE_API_CLIENTES_BASE_URL_HOMOLOG }}
          VITE_API_COMUNICACOES_BASE_URL_HOMOLOG: ${{ secrets.VITE_API_COMUNICACOES_BASE_URL_HOMOLOG }}
          VITE_API_CONTRATOS_BASE_URL_HOMOLOG: ${{ secrets.VITE_API_CONTRATOS_BASE_URL_HOMOLOG }}
          VITE_API_PESSOAS_BASE_URL_HOMOLOG: ${{ secrets.VITE_API_PESSOAS_BASE_URL_HOMOLOG }}
        run: |
          mkdir -p ~/.ssh
          echo "$VPS_SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
          
          ssh -i ~/.ssh/deploy_key ${VPS_USER}@${VPS_HOST} bash << ENDSSH
            set -e
            
            # Criar diretório se não existir
            mkdir -p ${VPS_DEPLOY_PATH}
            cd ${VPS_DEPLOY_PATH}
            
            # Verificar se é um repositório git, se não for, clonar
            if [ ! -d .git ]; then
              echo "📥 Clonando repositório..."
              # Se o diretório não está vazio e não é um git repo, fazer backup e limpar
              if [ "$(ls -A ${VPS_DEPLOY_PATH} 2>/dev/null)" ]; then
                echo "⚠️  Diretório não está vazio mas não é um repositório git. Fazendo backup..."
                cd ..
                mv ${VPS_DEPLOY_PATH} ${VPS_DEPLOY_PATH}.backup.$(date +%s)
              fi
              # Clonar repositório
              if [ -d ${VPS_DEPLOY_PATH} ]; then
                cd ..
                rm -rf ${VPS_DEPLOY_PATH}
              fi
              git clone https://github.com/Marshall-TDS/app-erp.git ${VPS_DEPLOY_PATH}
              cd ${VPS_DEPLOY_PATH}
            else
              echo "✅ Repositório git já existe"
            fi
            
            # Exportar variáveis de ambiente ANTES de executar docker-compose
            export VITE_API_USUARIOS_BASE_URL_HOMOLOG="${VITE_API_USUARIOS_BASE_URL_HOMOLOG}"
            export VITE_API_CLIENTES_BASE_URL_HOMOLOG="${VITE_API_CLIENTES_BASE_URL_HOMOLOG}"
            export VITE_API_COMUNICACOES_BASE_URL_HOMOLOG="${VITE_API_COMUNICACOES_BASE_URL_HOMOLOG}"
            export VITE_API_CONTRATOS_BASE_URL_HOMOLOG="${VITE_API_CONTRATOS_BASE_URL_HOMOLOG}"
            export VITE_API_PESSOAS_BASE_URL_HOMOLOG="${VITE_API_PESSOAS_BASE_URL_HOMOLOG}"
            
            # Fazer checkout da branch homolog
            git fetch origin
            git checkout homolog || git checkout -b homolog origin/homolog
            git pull origin homolog
            
            # Parar e remover container antigo
            docker-compose stop app-erp-homolog || true
            docker-compose rm -f app-erp-homolog || true
            
            # Build e iniciar container (as variáveis já estão exportadas)
            docker-compose build app-erp-homolog
            docker-compose up -d app-erp-homolog
            
            # Limpar imagens antigas
            docker image prune -f
            
            # Verificar status
            sleep 5
            docker-compose ps app-erp-homolog
            echo "✅ Deploy do App ERP (homolog) concluído com sucesso!"
          ENDSSH
